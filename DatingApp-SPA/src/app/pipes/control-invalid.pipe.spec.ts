import { ControlInvalidPipe } from './control-invalid.pipe';

describe('ControlInvalidPipe', () => {
  it('create an instance', () => {
    const pipe = new ControlInvalidPipe();
    expect(pipe).toBeTruthy();
  });
});
